<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/main.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>Lern</title>
    <style>
        * {
            font-family: sans-serif;
        }
        body {
            display: flex;
            justify-content: center;
        }
        .container {
            width: 100%;
            height: 70%;
            display: flex;
            justify-content: center;
        }
        .btn-rand {
            min-width: 60%;
        }
        .custom-cont {
            position: absolute;
            width: 100%;
            height: 80%;
        }
        .cont-word {
            width: max-content;
            height: max-content;
            border: 2px solid #cccccc;
            border-radius: 8px;
            max-width: 90vw;
        }

        @media screen and (max-width: 800px) {
            .btn-rand {
                width: 80vw;
            }
            .cont-word {
                overflow-x: scroll;
            }
            .helper {
                overflow-x: none;
            }
            .navbar {
                height: 9vh;
            }
        }
        @media screen and (max-width: 992px) {
            .nav-item {
                margin-right: 18px;
                font-size: 18px;
                font-weight: 550;
            };
            .input-container {
                margin-top: 200px;
            };
        }
        .bg-custom {
            background-color: #6CD4FF;
        }
        .navbar {
            position: absolute;
            top: 0;
            width: 100vw;
            max-height: 12vh;
            box-shadow: 0 2px 4px rgba(0, 0, 0, .2);
        }
        .navbar-nav {
            position: absolute;
            flex-direction: row!important;
            font-family: sans-serif;
            font-size: 19px;
            right: 0px;
        }
        a {
            color: white!important;
        }
        .nav-item {
            margin-right: 40px;
        }
        P {
            margin: auto;
        }
        h2 {
            position: relative;
            font-family: 'Raleway', sans-serif;
            margin-bottom: 250px;
        }
        #p-w {
            min-width: 150px;
            min-height: 27px;
            text-align: center;
            font-size: 19px;
            font-weight: 600;
        }
        .helper {
            min-width: 200px;
            min-height: 40px;
            overflow-x: none;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg p-2 navbar-light bg-custom">
        <a class="navbar-brand ml-3" href="/"><h2>Eng.</h2></a>
        <div class="navbar-nav mr-2">
            <a class="nav-item nav-link" href="/">Home</a>
            <a class="nav-item nav-link" href="/words">Words</a>
        </div>
        </div>
    </nav>
    <div class="container mt-5 p-3">
        <h2 class="mb-4">Get random word</h2>
        <div class="container cont-word p-2 mt-3">
            <p id="p-w">{{ word }}</p>
        </div>
        <div :style="styles.helper" @click="showHelper" class="container cont-word helper p-3 m-3">
            {{ translation }}
        </div>
        <button type="button" @click="getRandomWord" class="btn mt-5 btn-rand btn-outline-success p-2">Random</button>
    </div>
</body>
<script type="module" src="/scripts/lern.js"></script>
</html>