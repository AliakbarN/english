<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/main.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>Words List</title>
    <style>
        * {
            font-family: sans-serif;
        }
        body {
            display: flex;
            justify-content: center;
        }
        .container {
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: center;
        }
        .custom-cont {
            position: absolute;
            width: 100%;
            height: 80%;
        }

        @media screen and (max-width: 800px) {
            .btn-11 {
                margin-top: 250px;
                width: 80vw;
            }
            #add-btn {
                position: absolute;
                bottom: 30px;
                width: 80vw;
            }
            .custom-ul {
                width: 100%;
                height: 100%;
            }
            .li-span-text {
                overflow-x: scroll;
            }
            .nav-item {
                margin-right: 18px;
                font-size: 18px;
                font-weight: 550;
            };
            .input-container {
                margin-top: 200px;
            };
            .navbar {
                height: 9vh;
            }
        }
        @media screen and (max-width: 992px) {
            .nav-item {
                margin-right: 18px;
                font-size: 18px;
                font-weight: 550;
            };
            .input-container {
                margin-top: 200px;
            };
            .navbar {
                height: 9vh;
            }
        }
        .bg-custom {
            background-color: #6CD4FF;
        }
        .navbar {
            position: absolute;
            top: 0;
            width: 100vw;
            max-height: 12vh;
            box-shadow: 0 2px 4px rgba(0, 0, 0, .2);
        }
        .navbar-nav {
            position: absolute;
            flex-direction: row!important;
            font-family: sans-serif;
            font-size: 19px;
            right: 0px;
        }
        a {
            color: white!important;
        }
        .nav-item {
            margin-right: 40px;
        }
        P {
            margin: auto;
        }
        .custom-ul {
            min-width: 85%;
            height: 100%;
        }
        .list-group-item {
            transition: .3s;
        }
        .list-group-item:hover {
            background-color: #6e658d;
        }
        #add-btn {
            min-width: 43%;
            font-size: 17px;
            position: absolute;
            bottom: 30px;
        }
        .modal {
            display: none;
            top: 150px;
        }
        .spn-li {
            position: absolute;
            right: 0px;
        }
        .li-span-text {
            position: relative;
            width: 75%;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg p-2 navbar-light bg-custom">
        <a class="navbar-brand ml-3" href="/"><h2>Eng.</h2></a>
        <div class="navbar-nav mr-2">
            <a class="nav-item nav-link" href="/">Home</a>
            <a class="nav-item nav-link" href="/lern">Repeat words</a>
        </div>
        </div>
    </nav>
    <div class="modal" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
          <div class="modal-content">
            <div class="modal-body">
              <form>
                <div class="form-group">
                  <label for="recipient-name" class="col-form-label">{{ headerOfModal }}</label>
                  <input :value="inputValue.addWord" @input="changeInputData('save', $event)" type="text" class="form-control mt-3" :placeholder="modalWindowPlc" id="recipient-name">
                </div>
              </form>
            </div>
            <div class="modal-footer">
              <button type="button" @click="closeModal" class="btn btn-secondary" data-dismiss="modal">Close</button>
              <button type="button" @click="saveWord" class="btn btn-primary">Save</button>
            </div>
          </div>
        </div>
      </div>

    <div class="container d-flex custom-cont pt-5 p-3">
        <h3 class="mb-3">{{ title }}</h3>
        <ul class="list-group overflow-auto custom-ul p-3">
            <li class="list-group-item" @click="redirectToTranslatorOrSave(word)" v-for="word in words">
                <span class="d-inline li-span-text">
                    {{ word }}
                </span>
                <span class="spn-li mr-2 d-inline">
                    <img @click.stop="editTr(word)" class="mr-2" src="/stylesheets/icons/edit.png" alt="Edit">
                    <img @click.stop="deleteTr(word)" src="/stylesheets/icons/delete.png" class="" alt="Delete">
                </span>
            </li>
        </ul>
    </div>
    <button type="button" @click="setAddWord" id="add-btn" class="btn mt-5 p-2 btn-primary">{{ addWordBtnText }}</button>
</body>
    <script type="module" src="/scripts/words.js"></script>
</html>